<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>PawPlate</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<!-- Dev convenience: allow cleartext HTTP to the localhost dev API
	     (http://localhost:3000). App Transport Security blocks plain http by
	     default, which is why the iOS WebView reports "access control checks" /
	     "could not connect" for the REST calls. NSAllowsLocalNetworking permits
	     loopback/.local only — it does NOT weaken ATS for the public internet.
	     For a physical device on a LAN IP, add an NSExceptionDomains entry; for
	     store builds, point VITE_API_BASE_URL at the https deployment and drop
	     this (mirrors the cleartext note in capacitor.config.ts). -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- Capacitor's UI runs in a WKWebView, and fetch() from web content is
		     governed by NSAllowsArbitraryLoadsInWebContent — the direct parallel
		     to Android's allowMixedContent (capacitor.config.ts). This is what
		     lets the WebView reach the cleartext localhost dev API;
		     NSAllowsLocalNetworking / NSExceptionDomains alone don't cover
		     WebView-originated loads. Drop for store builds (point
		     VITE_API_BASE_URL at the https deployment). -->
		<key>NSAllowsArbitraryLoadsInWebContent</key>
		<true/>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<!-- Google Sign-In callback: replace the string below with your iOS OAuth
	     client's REVERSED client ID (Google Cloud console -> iOS client ->
	     "iOS URL scheme", e.g. com.googleusercontent.apps.1234567890-abcdef). -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>com.googleusercontent.apps.326034172026-7hts01pv3ipoku2fstgipl4taf65jv7r</string>
			</array>
		</dict>
	</array>
	<key>NSCameraUsageDescription</key>
	<string>PawPlate uses the camera to photograph your dog's bowl so it can identify the food and estimate portions.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>PawPlate lets you pick a bowl photo from your library to identify the food and estimate portions.</string>
</dict>
</plist>
