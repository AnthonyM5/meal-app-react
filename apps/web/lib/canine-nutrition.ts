// Re-export shim: the engine moved to packages/core (mobile phasing step 3).
// Keeps every existing `@/lib/canine-nutrition` import working unchanged.
export * from '@pawplate/core/canine-nutrition'
