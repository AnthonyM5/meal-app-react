// Re-export shim: safety data moved to packages/core (mobile phasing step 3).
// Keeps every existing `@/lib/dog-toxic-foods` import working unchanged.
export * from '@pawplate/core/dog-toxic-foods'
