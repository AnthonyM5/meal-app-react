packages:
  - apps/*
  - packages/*

# pnpm 10 blocks dependency lifecycle scripts unless approved here.
onlyBuiltDependencies:
  - cypress
  # vite's bundler — its postinstall places the platform binary
  - esbuild
